import React from 'react'

import Astroid from './Astroid'
const Space = ({ offsetX }) => {
    return (
        <div>
            <Astroid offsetX={offsetX}/>
        </div>
    )
}

export default Space
